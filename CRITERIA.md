# Done Criteria

- [ ] Prisma schema validation passes (no url in datasource)
- [ ] TypeScript check passes (no .next/types reference)
- [ ] Production build succeeds (no _document error)
- [x] Env var warnings resolved ✓ Set DATABASE_URL and STRIPE_SECRET_KEY as managed app secrets. Build no longer warns about missing e